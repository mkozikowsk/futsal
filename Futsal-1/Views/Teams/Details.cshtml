@model Futsal_1.Models.Team

@{
    ViewBag.Title = "Details";
    PageData["DropdownValue"] = 2;
}

<div>
    <h3>@Html.DisplayFor(model => model.Name)</h3>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Pełna nazwa
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Coach.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Coach.FullName)
        </dd>
    </dl>
</div>

<div class="table-responsive">
    <table class="table">
        <tr>
            <th class="col-xs-2">
                Zawodnik
            </th>
            <th class="col-xs-2"> 
                Wiek
            </th>
            <th class="col-xs-2">
                Wzrost
            </th>
            <th class="col-xs-2">
                Waga
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model.Players)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.FullName, "../Players/Details", new {id = item.Id})
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Height)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Weight)
                </td>
            </tr>
        }

    </table>
</div>
<p>
    @*@Html.ActionLink("Dodaj zawodnika", "..//Players/Create",new {@id = Model.Id}) |*@
    @Html.ActionLink("Dodaj zawodnika", "Create","Players", new { @id = Model.Id }, null) |
    @Html.ActionLink("Edit", "Edit", new {id = Model.Id}) |
    @Html.ActionLink("Back to List", "Index")
</p>
